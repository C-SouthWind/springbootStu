<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chj.mapper.ImportUserMapper">

    <insert id="inserts">
        insert into appuser (id,name,email,phone,gender,password,age)
        values (#{id},#{name},#{email},#{phone},#{gender},#{password},#{age})
    </insert>
</mapper>
